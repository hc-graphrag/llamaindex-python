# GraphRAG with LlamaIndex and Anthropic - Environment Variables

# Anthropic API Key (Required ONLY when using Anthropic provider directly)
# NOT needed when using AWS Bedrock
# Get your API key from: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# AWS Credentials (Required when using Bedrock provider)
# Bedrock does NOT require ANTHROPIC_API_KEY
# Option 1: Use AWS Profile (recommended)
# AWS_PROFILE_NAME=your-aws-profile-name
# Option 2: Use explicit credentials
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
# AWS_SESSION_TOKEN=your-session-token-if-needed

# Optional: Set logging level
# LOG_LEVEL=INFO